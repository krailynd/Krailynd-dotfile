# aliasvault.env — example (sanitized)
# AliasVault reads secrets from ./secrets/* files — keep this file minimal
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
JWT_KEY=REPLACE_WITH_RANDOM_HEX_64
ENCRYPTION_KEY=REPLACE_WITH_RANDOM_HEX_64
PRIVATE_EMAIL_DOMAINS=
