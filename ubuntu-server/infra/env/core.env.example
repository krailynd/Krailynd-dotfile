# core.env — example (sanitized). Copy to ~/sahacloud-infra/env/core.env (chmod 600)
# Do not commit real values. Store in AliasVault or sops.

POSTGRES_DB=postgres
POSTGRES_USER=sahacloud_admin
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

NEXTCLOUD_DB_PASS=REPLACE_WITH_STRONG_PASSWORD
MATTERMOST_DB_PASS=REPLACE_WITH_STRONG_PASSWORD
OUTLINE_DB_PASS=REPLACE_WITH_STRONG_PASSWORD
