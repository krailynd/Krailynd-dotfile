# excalidraw — example stack (placeholder). Adapt from other stacks.
# Uses ${VAR} from ~/sahacloud-infra/env/excalidraw.env — never hardcode secrets.
networks:
  sahacloud-net:
    external: true
services:
  excalidraw:
    image: REPLACE_WITH_IMAGE:latest
    container_name: sahacloud-excalidraw
    restart: unless-stopped
    env_file:
      - /home/sahacloud/sahacloud-infra/env/excalidraw.env
    networks:
      - sahacloud-net
