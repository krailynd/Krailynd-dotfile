# penpot — example stack (placeholder). Adapt from other stacks.
# Uses ${VAR} from ~/sahacloud-infra/env/penpot.env — never hardcode secrets.
networks:
  sahacloud-net:
    external: true
services:
  penpot:
    image: REPLACE_WITH_IMAGE:latest
    container_name: sahacloud-penpot
    restart: unless-stopped
    env_file:
      - /home/sahacloud/sahacloud-infra/env/penpot.env
    networks:
      - sahacloud-net
